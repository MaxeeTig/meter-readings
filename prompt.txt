# Target
Create mobile first web-ui to OCR and store meter readings 
# Source
Photo of meters made with mobile phone uploaded via mobile browser
# Destination
Local database (json) with dates and readings by meter type
Possibility to create reports and build graphs 
# Meters
- Electricity
- Hot water
- Cold water
# LLM Provider
Openrouter
# LLM Models
Cheap vision model 
# Output 
structured output
- readings
- indentification of meter as in # Meters section
# Sample photo 
./electricity_meter.jpg
./hot_water_meter.jpg
./cold_water_meter.jpg

1. Platform/runtime: do you want this as a single local app (frontend + backend), and if so, preferred
     stack (Next.js, React + Express, Python/FastAPI, etc.)?
 2. OCR flow: should OCR run server-side only (recommended, keeps OpenRouter key hidden), or do you also
     want optional on-device preprocessing?
  3. Database format: is a single JSON file acceptable (e.g. data/readings.json), or do you want
     lightweight SQLite with JSON export?
  4. Reading schema: should each record include timestamp, meter_type, reading_value, unit, image_path,
     confidence, and raw_model_output?
  5. Meter identification: should the model auto-detect meter type from image only, or should user select
     meter type in UI with model verification?
  6. Editing/approval: should users confirm/edit extracted reading before save (recommended)?
  7. Reports: what reports are mandatory first version: list/table only, monthly consumption deltas, and
     line charts by meter?
  8. Multi-user/auth: is this single-user local app with no login?
  9. Language/locale: UI and date/number formats in English or Russian (or both)?
  10. Data retention: should uploaded photos be stored permanently, compressed, or deleted after OCR?
  11. Error handling: when OCR is uncertain, should app block saving, warn with low-confidence badge, or
     allow manual override?
  12. Deployment target: run locally on your machine only, or containerized (Docker) for home server/NAS?




1. Single local app (frontend + backend) Python/FastAPI
2. OCR server side only 
3. Single JSON file acceptable 
4. Reading schema: in general, yes. not sure about image path cause we delete them see q.10 
btw is it possible to get date from jpg metadata - it will be proved source of truth. 
if not - we may take date from file name - format of name from smartphone: IMG_20260215_183948.jpg

5. Meter identification: better llm to detect meter type - there is label on photo above readings 
    - `СВУ-15И' `for hot water meter (round form-factor)
    - `СВХ-15И' `for cold water meter (round form-factor)
    - `electricity meter is labeled 'Меркурий 202.1' under readings (square form-factor)
6. Editing/approval: yes 
7. Reports: line charts by meter 
8. Single user 
9. Language/locale: russian pnly 
10. After success ocr images should be deleted 
11. User should be allowed to correct values before storing if ocr uncertain
12. Test run here on nootebook. Target service will be run on local intel nuc server in local network behind fw and nat 
suppose 'plain' deploy with no docker 

===

suggest to improve recognition. 1. send to llm previous reading for each meter 2. send to llm average
consumption. 3. ask llm to estimate reading and correct accordingly. i mean if recognition returned
36780 and previous was 364 something is definetly wrong  - in this case two of three red digits
recognized and added to result (fractions of m3)

=== 
@mosenergosbyt-api-reference.md
our target is not only to keep readings locally but also submit them to provider 
let us discuss flow for water meters submit. electricity will be discussed later - separate provider.
we investigated our mosenergosbyt portal and collected api's 
Need to update backend and frontend
Need to add functions for authorization of user in mosenergosbyt portal with login/password and storing session id.
also handle edge case with OTP login from new device
I think some menu item or button needed and probably state "authorized or not"
If not authorized at mosenergosbyt the system works as is 
In authorized mode need to show separate frame /card get key parameters of meters and display to user for cross-check.
At the moment submit flow and api is not known - will be provided later after 14 March.
Please ask questions and let us plan

*****
Questions to finalize scope

  1. Is this app single-user on one machine, or multi-user (accounts per user)?
  2. Should we store Mosenergosbyt login/password at all, or only session/device token and ask password again when needed?
  3. If credentials are stored, what security level do you want now: plain .env/json, or encrypted at rest?
  4. OTP UX: should we support method selection (flashcall/sms/email) or default to one method?
  5. Disconnect behavior: remove only session, or also delete stored device token?
  6. Do you want auto-refresh of provider meters (e.g., every app open), or only manual “Refresh” button?
  7. For cross-check card, which fields are mandatory to display first?
  8. Should provider auth status be global in header, or only inside a dedicated settings/integrations section?
  If you answer these, I can convert this into a concrete implementation plan and start coding immediately.

=== answers 

1. single user 
2. Store session and device token, ask for password only if requred (e.g. session dropped by portal)
3. No strage of user login and pasword
4. Default to SMS, possibility to change somewhere on config. Option to change will be rarely used only if celluar operator fail 
5. I think better leave stored device token because it is conected to device
6. Let us auto-refresh if user  authorized - lesser buttons in UI
7. Our target is to store actual readings so user should ensure that readings are 1. for correct meter 2. stored properly so i think the following order in card:
- Meter label (e.g. Hot Water)
-   "nm_counter": "Г 49065" # this is internal reg. number for user 
-   "vl_last_indication": 364, # last readings 
-   "dt_last_indication": "2026-02-15 00:00:00.0" # last readings date 
8. Maybe we'll do without a separate indicator? If user will clearly understand if it is authorized in portal maybe special status not required.
What do you think. If we have separate card for meters from portal and user not authorized in portal we place 'Login' or 'Authorize' button there?
So after authorization user will see readings from portal - if not understood that auth required and press button? 












